import type{Metadata}from'next';import{notFound}from'next/navigation';import{getAlbums}from'@/lib/data';import{PhotoGrid}from'@/components/site/lightbox';import{PageShell}from'@/components/site/page-shell';
export const dynamic='force-dynamic';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=(await getAlbums()).find(x=>x.slug===slug);return a?{title:a.title,description:a.description,openGraph:{title:`${a.title} | Airsoft Armer Team`,description:a.description,images:[a.cover]}}:{title:'Target Not Found'}}
export default async function AlbumPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=(await getAlbums()).find(x=>x.slug===slug);if(!a)notFound();return <PageShell><section className="album-head shell"><p className="eyebrow">FIELD ARCHIVE // {a.isDemo?'DEMO ALBUM':'AAT'}</p><h1>{a.title}</h1><div><span>{new Date(a.date).toLocaleDateString('ro-RO')}</span><span>{a.location}</span><span>{a.photos.length} PHOTOS</span>{a.photographer&&<span>PHOTO CREDIT // {a.photographer}</span>}</div><p>{a.description}</p></section><section className="shell album-photos"><PhotoGrid photos={a.photos} downloadEnabled={a.downloadEnabled}/></section></PageShell>}
