import type{Metadata}from'next';import{getAlbums}from'@/lib/data';import{AlbumCard}from'@/components/site/cards';import{PageHero,PageShell}from'@/components/site/page-shell';
export const metadata:Metadata={title:'Field Archive',description:'Albumele foto Airsoft Armer Team.'};
export default async function GalleryPage(){const albums=await getAlbums();return <PageShell><PageHero eyebrow="AAT // ARCHIVE" title="FIELD ARCHIVE" subtitle="THE OPERATIONS. THE PEOPLE. THE MOMENTS." image="/images/hero-demo.png"/><section className="listing shell"><div className="filters"><button className="active">ALL YEARS</button><button>LOCATION</button><button>EVENT</button><button>MEMBER</button></div>{albums.length?<div className="album-grid">{albums.map(a=><AlbumCard key={a.id} album={a}/>)}</div>:<div className="empty">NO FIELD INTEL FOUND</div>}</section></PageShell>}
