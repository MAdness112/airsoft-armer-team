import type{Metadata}from'next';import{notFound}from'next/navigation';import{getAlbums,getMembers}from'@/lib/data';import{PageShell}from'@/components/site/page-shell';import{AlbumCard}from'@/components/site/cards';
export async function generateStaticParams(){return(await getMembers()).map(m=>({slug:m.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const m=(await getMembers()).find(x=>x.slug===slug);return m?{title:m.callsign,description:m.shortBio,openGraph:{title:`${m.callsign} | Airsoft Armer Team`,description:m.shortBio,images:m.heroImage?[m.heroImage]:[]}}:{title:'Target Not Found'}}
export default async function MemberPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const all=await getMembers();const m=all.find(x=>x.slug===slug);if(!m)notFound();const albums=(await getAlbums()).filter(a=>a.memberIds.includes(m.id));const facts=[['CALLSIGN',m.callsign],['FULL NAME',`${m.firstName} ${m.lastName}`],['ROLE',m.role],['MEMBER SINCE',m.memberSince],['STATUS',m.status.toUpperCase()],['OPERATOR ID',m.operatorId]].filter((x):x is[string,string]=>Boolean(x[1]));const loadout=[['PRIMARY',m.primaryReplica],['SECONDARY',m.secondaryReplica],['GEAR',m.gear],['SPECIALTY',m.specialty],['PLAY STYLE',m.playStyle]].filter((x):x is[string,string]=>Boolean(x[1]));return <PageShell><section className="dossier-hero"><img src={m.heroImage??m.profileImage} alt={`Portret ${m.callsign}`}/><div className="dossier-shade"/><div className="shell dossier-head"><p>OPERATOR DOSSIER // {m.isDemo?'DEMO RECORD':'AAT'}</p><h1>{m.callsign}</h1></div></section><section className="dossier shell"><div className="dossier-facts">{facts.map(([k,v])=><div key={k}><small>{k}</small><b>{v}</b></div>)}</div><article><p className="eyebrow">BIOGRAPHY</p><h2>BEHIND THE CALLSIGN.</h2><p>{m.bio??m.shortBio}</p>{m.motto&&<blockquote>“{m.motto}”</blockquote>}</article>{loadout.length>0&&<div className="loadout"><p className="eyebrow">OPERATOR LOADOUT</p>{loadout.map(([k,v])=><div key={k}><small>{k}</small><b>{v}</b></div>)}</div>}</section>{albums.length>0&&<section className="content-section shell"><div className="section-heading"><p className="eyebrow">FIELD ARCHIVE</p><h2>TAGGED INTEL</h2></div><div className="album-grid">{albums.map(a=><AlbumCard key={a.id} album={a}/>)}</div></section>}</PageShell>}
