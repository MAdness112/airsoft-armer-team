'use client';
import Link from 'next/link';
import { Menu, X } from 'lucide-react';
import { useEffect, useState } from 'react';
import { LanguageToggle, useLanguage } from './language-provider';
const links=[{ro:'ACASĂ',en:'HOME',href:'/'},{ro:'ECHIPA',en:'TEAM',href:'/team'},{ro:'JOCURI',en:'OPERATIONS',href:'/operations'},{ro:'GALERIE',en:'GALLERY',href:'/gallery'},{ro:'DESPRE NOI',en:'ABOUT US',href:'/about'},{ro:'CONTACT',en:'CONTACT',href:'/contact'}];
export function Navbar(){const{locale}=useLanguage();const[open,setOpen]=useState(false);const[scrolled,setScrolled]=useState(false);useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>40);onScroll();window.addEventListener('scroll',onScroll,{passive:true});return()=>window.removeEventListener('scroll',onScroll)},[]);return <header className={`navbar ${scrolled?'scrolled':''}`}><Link href="/" className="brand" aria-label={`Airsoft Armer Team — ${locale==='ro'?'Acasă':'Home'}`}><span>AAT</span><small>AIRSOFT<br/>ARMER TEAM</small></Link><nav aria-label={locale==='ro'?'Navigație principală':'Main navigation'}>{links.map((link)=><Link key={link.href} href={link.href}>{link[locale]}</Link>)}</nav><LanguageToggle/><div className="nav-status"><span/> AAT ONLINE</div><button className="menu-trigger" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="mobile-menu">{open?<X/>:<Menu/>}<span>{locale==='ro'?'MENIU':'MENU'}</span></button><div id="mobile-menu" className={`mobile-menu ${open?'open':''}`}><p>AAT // {locale==='ro'?'NAVIGAȚIE':'NAVIGATION'}</p><LanguageToggle/>{links.map((link,i)=><Link key={link.href} href={link.href} onClick={()=>setOpen(false)}><span>0{i+1}</span>{link[locale]}</Link>)}<small>FUNDU MOLDOVEI // ROMANIA</small></div></header>}
