'use client';
import{useEffect}from'react';import{z}from'zod';import{getSupabase,isDemoMode}from'@/lib/supabase';
type Tool={name:string;title:string;description:string;inputSchema:object;annotations:{readOnlyHint:boolean;untrustedContentHint:boolean};execute:(input:unknown)=>Promise<unknown>};
declare global{interface Document{modelContext?:{registerTool:(tool:Tool,options?:{signal?:AbortSignal})=>void|Promise<void>}}}
const schema=z.object({name:z.string().min(2).max(100),email:z.string().email(),subject:z.enum(['JOIN THE TEAM','PLAY WITH US','EVENT INVITATION','COLLABORATION','OTHER']),message:z.string().min(5).max(5000)});
export function WebMcpContact(){useEffect(()=>{const context=document.modelContext;if(!context?.registerTool)return;const lifecycle=new AbortController();const tool:Tool={name:'submit_aat_contact_message',title:'Trimite mesaj către AAT',description:'Trimite un mesaj prin formularul oficial Airsoft Armer Team.',inputSchema:{type:'object',properties:{name:{type:'string',minLength:2,maxLength:100},email:{type:'string',format:'email'},subject:{type:'string',enum:['JOIN THE TEAM','PLAY WITH US','EVENT INVITATION','COLLABORATION','OTHER']},message:{type:'string',minLength:5,maxLength:5000}},required:['name','email','subject','message'],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:true},async execute(input){const value=schema.parse(input);if(!isDemoMode){const{error}=await getSupabase()!.from('contact_messages').insert(value);if(error)throw new Error('Message could not be sent')}window.dispatchEvent(new CustomEvent('aat-contact-sent'));return{status:isDemoMode?'demo':'received'}}};try{void Promise.resolve(context.registerTool(tool,{signal:lifecycle.signal})).catch(()=>undefined)}catch{return}return()=>lifecycle.abort()},[]);return null}
